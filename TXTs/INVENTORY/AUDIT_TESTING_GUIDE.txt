Inventory Module - Audit Testing Guide
Date: December 3, 2025  
Status: All 13 audit logging points implemented and ready for testing
---
Quick Test Checklist
Use this guide to verify all audit logging implementations are working correctly.
Items Management (3 Tests)
Test 1.1: Item Creation
Go to Inventory → Items
Click "Create Item"
Fill in: Name, Category, UOM, Reorder Level, Max Stock
Click Save (if super admin) or Submit for Approval
Verify: Check auditlogs table
   bash
   php artisan tinker
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\Item')
        ->where('actiontype', 'create')
        ->latest()
        ->first();
   
Expected: Log with item name, SKU, category, UOM in description
Test 1.2: Item Update
Go to Inventory → Items
Click Edit on any item
Change name, category, UOM, or reorder level
Save changes
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\Item')
        ->where('actiontype', 'update')
        ->latest()
        ->first();
   
Expected: Log showing what changed (e.g., "Name: Old → New")
Test 1.3: Item Deletion
Go to Inventory → Items
Click Delete on any item
Confirm deletion
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\Item')
        ->where('actiontype', 'delete')
        ->latest()
        ->first();
   
Expected: Log with deleted item name and SKU
---
Purchases (2 Tests)
Test 2.1: Purchase Creation
Go to Inventory → Purchases
Click "Create Purchase"
Fill in: Supplier, Date, Add items with quantities and costs
Click Save
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\Purchase')
        ->where('actiontype', 'create')
        ->latest()
        ->first();
   
Expected: Log with purchase number, supplier, FOB costs, landing cost, item count
Test 2.2: Purchase Deletion
Go to Inventory → Purchases
Find a purchase and click Delete
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\Purchase')
        ->where('actiontype', 'delete')
        ->latest()
        ->first();
   
Expected: Log with purchase number, supplier, item count, landing cost
---
Stocks (2 Tests)
Test 3.1: Stock Adjustment Request (Non-Admin)
Go to Inventory → Stocks
Click Edit on any stock
Change quantities (available, reserved, damaged)
Click Save
Enter reason for adjustment in audit modal
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\Stock')
        ->where('actiontype', 'update')
        ->where('status', 'pending')
        ->latest()
        ->first();
   
Expected: Log with status='pending', item name, quantities, and reason
Test 3.2: Stock Direct Update (Super Admin)
Log in as super admin
Go to Inventory → Stocks
Click Edit on any stock
Change quantities
Click Save (no audit modal for admins)
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\Stock')
        ->where('actiontype', 'update')
        ->where('status', 'completed')
        ->latest()
        ->first();
   
Expected: Log with status='completed', changes listed
---
Item Requests (1 Test)
Test 4.1: Item Request Creation
Go to Inventory → Item Requests
Click "Create Request"
Select Department
Add Items with quantities
Click Save
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\ItemRequest')
        ->where('actiontype', 'create')
        ->latest()
        ->first();
   
Expected: Log with request number, department name, item count, request date
---
Item Dispatches (2 Tests)
Test 5.1: Item Approval
Go to Inventory → Item Dispatches
Click on a pending request in the accordion
Enter approve quantities for items
Click "Approve Items"
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\ItemRequest')
        ->where('actiontype', 'update')
        ->orderBy('createdat', 'desc')
        ->first();
   
Expected: Log with "Approved X item(s)" and item names with quantities
Test 5.2: Item Dispatch
From the same request in dispatch modal
Confirm items are approved
Click "Dispatch Items"
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\ItemRequest')
        ->where('actiontype', 'update')
        ->orderBy('createdat', 'desc')
        ->limit(1)
        ->first();
   
Expected: Log with "Dispatched items from request" and status updates
---
Stock Takes (2 Tests)
Test 6.1: Stock Take Creation
Go to Inventory → Stock Takes
Click "Create Stock Take"
Select type (full/partial/cycle)
Enter physical quantities for items
Click Save
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\StockTake')
        ->where('actiontype', 'create')
        ->latest()
        ->first();
   
Expected: Log with stock take number, type, items counted, variance details
Test 6.2: Stock Take Completion
Go to Inventory → Stock Takes
Find a stock take with status "inprogress"
Click "Complete"
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\StockTake')
        ->where('actiontype', 'update')
        ->latest()
        ->first();
   
Expected: Log with "Completed stock take" and matched/surplus/shortage counts
---
Health Checks (1 Test)
Test 7.1: Health Check Creation
Go to Inventory → Health Checks
Click "Create Health Check"
Select Stock
Fill in: Date, Condition, Quantity Affected, Observations, Action
Click Save
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\HealthCheck')
        ->where('actiontype', 'create')
        ->latest()
        ->first();
   
Expected: Log with item name, condition, quantity affected, observations
---
Approval Request Tracking (4 Tests)
Test 8.1: Stock Adjustment Approval Request
Go to Inventory → Stocks
Click Edit on any stock
Change quantities (non-admin user)
Click Save and enter reason for audit
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\ApprovalAuditRequest')
        ->where('actiontype', 'create')
        ->where('status', 'pending')
        ->latest()
        ->first();
   
Expected: Log showing "Requested stock adjustment approval"
Test 8.2: Item Creation Approval Request
Go to Inventory → Items
Click Create (non-admin user)
Enter item details
Click Save and enter approval reason
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\ApprovalAuditRequest')
        ->where('actiontype', 'create')
        ->orderBy('createdat', 'desc')
        ->first();
   
Expected: Log showing "Requested item creation approval"
Test 8.3: Item Update Approval Request
Go to Inventory → Items
Click Edit (non-admin user)
Modify item details
Click Save and enter approval reason
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\ApprovalAuditRequest')
        ->where('actiontype', 'create')
        ->orderBy('createdat', 'desc')
        ->first();
   
Expected: Log showing "Requested item update approval"
Test 8.4: Item Deletion Approval Request
Go to Inventory → Items
Click Delete (non-admin user)
Enter approval reason
Verify:
   bash
   >>> DB::table('auditlogs')
        ->where('auditabletype', 'App\Models\ApprovalAuditRequest')
        ->where('actiontype', 'create')
        ->orderBy('createdat', 'desc')
        ->first();
   
Expected: Log showing "Requested item deletion approval"
---
Bulk Verification Query
Run this to see all inventory audits created:
bash
php artisan tinker
Count all inventory audits
>>> DB::table('auditlogs')
     ->whereIn('auditabletype', [
       'App\Models\Item',
       'App\Models\Purchase',
       'App\Models\Stock',
       'App\Models\ItemRequest',
       'App\Models\ItemDispatch',
       'App\Models\StockTake',
       'App\Models\HealthCheck'
     ])
     ->count();
View all with pagination
>>> DB::table('auditlogs')
     ->whereIn('auditabletype', [
       'App\Models\Item',
       'App\Models\Purchase',
       'App\Models\Stock',
       'App\Models\ItemRequest',
       'App\Models\ItemDispatch',
       'App\Models\StockTake',
       'App\Models\HealthCheck'
     ])
     ->orderBy('createdat', 'desc')
     ->paginate(20);
View specific component audits
>>> DB::table('auditlogs')
     ->where('auditabletype', 'App\Models\Item')
     ->orderBy('createdat', 'desc')
     ->paginate(10);
---
Expected Results After All Tests
Items:                3 creates + 1 update + 1 delete = 5 logs
Purchases:            1 create + 1 delete = 2 logs
Stocks:               1 pending + 1 completed = 2 logs
Item Requests:        1 create = 1 log
Item Dispatches:      1 approval + 1 dispatch = 2 logs
Stock Takes:          1 create + 1 update = 2 logs
Health Checks:        1 create = 1 log
Approval Requests:    4 (stock adj + item create + item update + item delete) = 4 logs
TOTAL:                ~21 audit logs
---
Troubleshooting
Issue: No audit logs appearing
Check 1: Verify AuditService exists
bash
ls -la app/Services/AuditService.php
Check 2: Verify currentactor() helper works
bash
php artisan tinker
>>> currentactor()
Check 3: Check auditlogs table exists
bash
php artisan tinker
>>> DB::table('auditlogs')->first();
Check 4: Check Laravel logs for errors
bash
tail -f storage/logs/laravel.log
Issue: Wrong actor in logs
Check: Verify currentactor() returns correct user
bash
php artisan tinker
>>> currentactor()
>>> Auth::guard('employees')->user()
Issue: Model class not found
Check: Verify morphClass is set in models
bash
Check Item model
cat app/Models/Item.php | grep morphClass
Should see: protected $morphClass = 'App\Models\Item';
---
Performance Check
After testing, verify no performance degradation:
bash
Monitor database queries
>>> DB::enableQueryLog();
>>> // Run an operation
>>> printr(DB::getQueryLog());
Check for N+1 queries
Should see only 1-2 extra queries for audit logging
---
Success Criteria
✅ All 13 audit points log correctly
✅ Logs show correct actor
✅ Descriptions are clear and detailed
✅ Action types match operation (create/update/delete)
✅ Status reflects workflow state
✅ No errors in logs
✅ No performance degradation
✅ All timestamps are accurate
---
Next Steps After Verification
✅ Test - Follow checklist above
✅ Verify - Run bulk verification queries
Document - Update team on audit capabilities
Deploy - Push to production with confidence
Monitor - Watch for any issues
Archive - Keep these test results for reference
---
Document Created: December 3, 2025  
Reference: AUDITIMPLEMENTATIONCOMPLETED.md  
Status: Ready for Testing
